# JSP views live in /WEB-INF/views/<name>.jsp. Boot's auto-configured view resolver picks
# them up from these two properties, so the example needs no view-resolver code.
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

server.port=8080
