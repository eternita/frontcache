# fc-elk configuration. Copy to `.env` and edit.
#   cp .env.example .env

# Elastic stack version (elasticsearch, kibana, logstash all pinned to this).
STACK_VERSION=8.15.3

# Heap sizes.
ES_HEAP=2g
LS_HEAP=1g

# Published host ports.
ES_PORT=9200
KIBANA_PORT=5601
